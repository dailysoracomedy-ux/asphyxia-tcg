/**
 * External community links, in one place so they're never scattered as
 * string literals through components.
 *
 * DISCORD_INVITE is the PERMANENT server invite (never expires, unlimited
 * uses) - also hardcoded in the Reply Copilot extension, so if it ever has
 * to change, change it in both places.
 */
export const DISCORD_INVITE = 'https://discord.gg/mg4T8682qs';
