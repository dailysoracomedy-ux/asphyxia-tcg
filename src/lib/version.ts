// Bump this string with every commit you push so the deployed game visibly confirms
// which version is actually live. Shown on the deck-select menu (NewGameMenu.tsx).
// Update this one line, then use the matching "Commit N" message on GitHub.
export const BUILD_VERSION = 'Commit 22';
